
# 📬 Email & Document Monitor

ระบบตรวจสอบอีเมลจาก **Outlook Web** และเอกสารจากระบบ **e-office** พร้อมแจ้งเตือนผ่าน **Telegram** แบบอัตโนมัติ 🚀

โปรเจกต์นี้ถูกออกแบบมาเพื่อช่วยให้คุณไม่พลาดทุกการสื่อสารที่สำคัญ โดยระบบจะคอยเช็คอีเมลและเอกสารใหม่ๆ ให้คุณ แล้วส่งข้อความแจ้งเตือนทันที เหมาะสำหรับผู้ที่ต้องการความรวดเร็วและไม่อยากล็อกอินเข้าระบบต่างๆ บ่อยครั้ง

-----

## 🔔 คุณสมบัติเด่น (Key Features)

  * **✉️ ตรวจสอบอีเมลใหม่:** ดึงอีเมลล่าสุดจาก Outlook Web (OWA) โดยอัตโนมัติ
  * **📑 ตรวจสอบเอกสารใหม่:** ตรวจเช็คเอกสารเข้าใหม่จากระบบ e-office
  * **📲 แจ้งเตือนผ่าน Telegram:** ส่งข้อความหาคุณทันทีเมื่อมีรายการใหม่ พร้อมรายละเอียดครบถ้วน
  * **🧠 ระบบ Cache อัจฉริยะ:** ป้องกันการแจ้งเตือนซ้ำสำหรับรายการที่เคยแจ้งไปแล้ว
  * **⚙️ ปรับแต่งได้ตามใจ:**
      * กำหนดจำนวนอีเมลและเอกสารสูงสุดที่ต้องการตรวจสอบ
      * เลือกเปิดหรือปิดการแจ้งเตือนของแต่ละระบบ (อีเมล/เอกสาร) ได้อย่างอิสระ
  * **🖥️ โหมดแสดงผลเบราว์เซอร์:** สามารถเลือกให้เบราว์เซอร์ทำงานเบื้องหลัง (headless) หรือแสดงหน้าต่างการทำงานได้
  * **📝 ระบบ Log ละเอียด:** ติดตามการทำงานของระบบได้ง่ายด้วย Log ที่ระบุเวลาและระดับความสำคัญ (INFO, DEBUG, ERROR)
  * **💻 รองรับ Windows:** มาพร้อมสคริปต์ `.bat` ช่วยให้การติดตั้งและรันโปรแกรมเป็นเรื่องง่าย

-----

## 📁 โครงสร้างโปรเจกต์ (Project Structure)

```
email-monitor/
├── .env                         # ไฟล์เก็บค่าตั้งค่าต่างๆ เช่น Token, Password, URL
├── main.py                      # สคริปต์หลักสำหรับรันโปรแกรม
├── requirements.txt             # รายการไลบรารีที่ต้องใช้
├── run_monitor.bat              # (Windows) สคริปต์สำหรับรันระบบ
├── setup_monitor.bat            # (Windows) สคริปต์สำหรับติดตั้งโปรแกรม
├── cache/
│   ├── notified_documents.json  # เก็บประวัติเอกสารที่แจ้งเตือนแล้ว
│   └── notified_subjects.json   # เก็บประวัติอีเมลที่แจ้งเตือนแล้ว
├── utils/
│   ├── telegram_notify.py       # โมดูลสำหรับส่งข้อความ Telegram
│   ├── logger.py                # โมดูลจัดการ Log
│   └── html_parser.py           # โมดูลช่วยประมวลผล HTML
├── email_checker.py             # โมดูลตรวจสอบอีเมล
└── document_checker.py          # โมดูลตรวจสอบเอกสาร
```

-----

## ⚙️ การติดตั้งและใช้งาน (Getting Started)

ทำตามขั้นตอนง่ายๆ เพียง 3 ขั้นตอนเพื่อเริ่มใช้งาน

### 1\. ตั้งค่า Environment (`.env`)

สร้างไฟล์ชื่อ `.env` ในโฟลเดอร์หลักของโปรเจกต์ แล้วคัดลอกเนื้อหาด้านล่างไปวาง จากนั้นแก้ไขค่าต่างๆ ให้เป็นข้อมูลของคุณ

```ini
# --- Telegram Settings ---
TELEGRAM_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# ---  Web Mails Settings ---
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_password
OWA_URL=https://webmail.example.com

EMAIL_USER=your_email@example.com
EMAIL_PASS=your_password
OWA_URL=https://webmail.example.com

# --- E-Office Settings ---
DOCUMENT_URL=http://e-office.example.com/login
DOCUMENT_USER=your_username
DOCUMENT_PASS=your_password

ESARABAN_URL=https://e-saraban.example.com/login
ESARABAN_USER=your_username
ESARABAN_PASS=your_password

# --- General Settings ---
LOG_LEVEL=INFO
MAX_EMAILS=5
MAX_DOCUMENTS=5
NOTIFY_EMAIL=true
NOTIFY_DOCUMENT=true
SHOW_BROWSER=false
```

**คำอธิบาย:**

  * `TELEGRAM_TOKEN`: Token ของ Bot ที่ได้จาก BotFather
  * `TELEGRAM_CHAT_ID`: ID ของแชทที่ต้องการให้ Bot ส่งข้อความไป
  * `SHOW_BROWSER`: ตั้งเป็น `true` หากต้องการเห็นหน้าต่างเบราว์เซอร์ขณะทำงาน (เหมาะสำหรับ Debug)

### 2\. ติดตั้ง Dependencies

คุณสามารถติดตั้งไลบรารีที่จำเป็นทั้งหมดได้ 2 วิธี:

  * **(แนะนำสำหรับ Windows)** ดับเบิลคลิกที่ไฟล์ `setup_monitor.bat` เพื่อติดตั้งอัตโนมัติ
  * **ติดตั้งด้วยตนเอง**ผ่าน Command Prompt หรือ Terminal:
    ```bash
    pip install -r requirements.txt
    ```

### 3\. รันระบบ

  * **(แนะนำสำหรับ Windows)** ดับเบิลคลิกที่ไฟล์ `run_monitor.bat` เพื่อเริ่มการทำงาน
  * **รันด้วยตนเอง**ผ่าน Command Prompt หรือ Terminal:
    ```bash
    python main.py
    ```

หลังจากรันโปรแกรม ระบบจะเริ่มตรวจสอบอีเมลและเอกสารตามรอบเวลา และส่งการแจ้งเตือนไปยัง Telegram ของคุณเมื่อพบรายการใหม่

-----

## 🛠️ ผู้พัฒนา (Author)

**phayao** - Fullstack Architect

  * เชี่ยวชาญด้านการพัฒนาระบบอัตโนมัติ (Automation), ระบบแจ้งเตือน (Notification), และการ Deploy ระบบสำหรับใช้งานจริง (Production-Grade Deployment) บน VPS